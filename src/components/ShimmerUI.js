const Shimmer=()=>{
    return (<div className="restaurant-list">
        {Array(15).fill("abc").map((x)=>{return(
            <div className="shimmer-card">
                
            </div>
        )
        })}
    </div>)
}

export default Shimmer;